{
  "doc": "Delays progress by a fixed amount. Applied via PartProgress's 'op'/'operation' field (e.g. `op: delay`), NOT 'type' - its parameter fields (if any) are written flat alongside 'op', on the same object.",
  "amount": {
    "type": "float",
    "doc": "Amount to delay progress by."
  }
}
