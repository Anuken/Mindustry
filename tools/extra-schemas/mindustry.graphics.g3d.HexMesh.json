{
  "superclass": "mindustry.graphics.g3d.PlanetMesh",
  "doc": "Not parsed via the mesh-type switch itself - falls through to the default (Planet, int divisions) constructor when 'type' is 'HexMesh'.",
  "divisions": {
    "type": "int",
    "doc": "Subdivision count for the generated hex grid."
  }
}
