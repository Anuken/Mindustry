{
  "doc": "Slopes progress: rises to 1 then falls back to 0. Applied via PartProgress's 'op'/'operation' field (e.g. `op: slope`), NOT 'type' - its parameter fields (if any) are written flat alongside 'op', on the same object."
}
